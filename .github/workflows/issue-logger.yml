name: Issue Logger
on:
   issues:
        types:
            - opened
            - closed
jobs:
    Print-Details:
        runs-on: ubuntu-latest
        steps:
            - name: Print gh object
              run: echo "${{toJSON(github.event)}}"