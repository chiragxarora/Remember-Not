name: first-workflow
on: push
jobs:
  first-job:
    name: first-job
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: echo "Hello World!!"
      - name: Print bye
        run: echo "Done, Bye!"
